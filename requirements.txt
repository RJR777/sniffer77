# Network sniffing and packet capture
scapy>=2.5.0

# Web dashboard (FastAPI-based)
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
websockets>=12.0
jinja2>=3.1.0

# OUI database - uses local Wireshark manuf file (no external dependency needed)

# Network scanning
netifaces>=0.11.0

# Database
# (using built-in sqlite3)
